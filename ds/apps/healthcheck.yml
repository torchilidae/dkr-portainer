services: 
  healthcheck: 
    image: alpine:3.20.3
    container_name: healthcheck
    restart: always
    network_mode: host
    command: /run.sh
    volumes:
      - /volume1/docker/conf/healthcheck/root:/etc/crontabs/root
      - /volume1/docker/conf/healthcheck/run.sh:/run.sh